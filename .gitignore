__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
*.db
*.sqlite
*.sqlite3
# Runtime SQLite/data dir at the repo root only — NOT bundled package data
# like app/scrub/data/ (anchored so app/**/data/ stays tracked).
/data/
.pytest_cache/
.streamlit/
.DS_Store
.idea/
.vscode/

# Rust / maturin build artifacts
target/
*.so
*.dylib
*.pyd
crates/*/dist/
# Cargo.lock IS committed (Python wheel is a binary deliverable, not a library).
